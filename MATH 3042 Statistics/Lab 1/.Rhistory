t = 16
t^2
2+2
knit_with_parameters("~/Documents/GitHub/CST_Semester_3/MATH 3042 Statistics/Lab 1/Lab_01_Notebook.Rmd")
install.packages("mosaic")
library(mosaicData)
library(mosaicData)
library(mosaicData)
data(TenMileRace)
library(mosaicData)
data(TenMileRace)
library(mosaicData)
data(TenMileRace)
if (!require("mosaicData", quietly=TRUE)) {
install.packages("dplyr")
library(dplyr)
if (!require("dplyr", quietly=TRUE)) {
install.packages("dplyr")
library(dplyr)
if (!require("dplyr", quietly=TRUE)) {
install.packages("dplyr")
}
library(dplyr)
View(TenMileRace)
View(TenMileRace)
help(TenMileRace)
nrow(TenMileRace)
TenMileRace[1, 2]
TenMileRace[1, "time"]
TenMileRace[c(1, 2, 3, 4, 5), "time"]
c(3, 6, 8, 10)
1:5
race.times <- TenMileRace$time
race.times[1:5]
TenMileRace[1:50, "state"]
TenMileRace[1:50, "state"]
TenMileRace$state[1:50]
TenMileRace$state=="Ethiopia"
sum(TenMileRace$state=="Ethiopia")
TenMileRace[TenMileRace$state=="Ethiopia"]
TenMileRace[TenMileRace$state=="Ethiopia", ]
TenMileRace[TenMileRace$net>10000, sex]
TenMileRace[TenMileRace$net>10000, "sex"]
slowRunners = TenMileRace[TenMileRace$net>10000, "sex"]
slowRunners
sum(slowRunners)
slowRunners = TenMileRace[TenMileRace$net>10000, "sex"]
slowRunners
count(slowRunners)
sum( TenMileRace$time < 3000)
slowRunners = TenMileRace[TenMileRace$net>10000, "sex"]
slowRunners
sum(slowRunners)
slowRunners = TenMileRace[TenMileRace$net>10000, "sex"]
slowRunners
length(slowRunners)
mid.runners = subset(TenMileRace, 100000 >= net >= 3000)
mid.runners = subset(TenMileRace, 100000 => net >= 3000)
mid.runners = subset(TenMileRace, 100000 => net => 3000)
mid.runners = subset(TenMileRace, 100000 >= net && net >= 3000)
mid.runners = subset(TenMileRace, 100000 >= net && net >= 3000)
mid.runners = subset(TenMileRace, 100000 >= net & net >= 3000)
length(mid.runners)
subset(TenMileRace, sex = "F" & state = "Russia" || state =  "Kenya")
subset(TenMileRace, sex = "F" & state == "Russia" || state =="Kenya")
subset(TenMileRace, sex = "F" & (state == "Russia" || state =="Kenya"))
subset(TenMileRace, sex == "F" & (state == "Russia" || state =="Kenya"))
subset(TenMileRace, sex == "F" & (state == "Russia" | state =="Kenya"))
subset(TenMileRace, length(state)==2)
subset(TenMileRace, length(as.character(state))==2)
subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
table(usa.runners$state)
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
table(usa.runners$state)
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
table(usa.runners$state)
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
which.max(usa.runners)
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
help("which.max")
which.max(usa.runners)
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
help("which.max")
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
help("which.max")
which.max(usa.runners)
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
which.max(usa.runners$state)
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners = which.max(usa.runners$state)
usa.runners$state[most.runners]
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners.state = which.max(usa.runners$state)
usa.runners$state[most.runners.state]
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners.state = which.max(sum(usa.runners$state)
usa.runners$state[most.runners.state]
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners.state = which.max(sum(usa.runners$state))
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners.state = which.max(usa.runners$state[1])
usa.runners$state[most.runners.state]
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners.state = which.max(usa.runners$state[0])
usa.runners$state[most.runners.state]
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners.state = which.max(usa.runners$state[1])
usa.runners$state[most.runners.state]
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
usa.runners$state
most.runners.state = which.max(usa.runners$state[1])
usa.runners$state[most.runners.state]
usa.runners = subset(TenMileRace, nchar(as.character(state))==2)
usa.runners <- droplevels( usa.runners )
most.runners.state = which.max(usa.runners$state[1])
usa.runners$state[most.runners.state]
m.runners = subset(TenMileRace, sex == "M")
f.runners = subset(TenMileRace, sex == "F")
m.runners = subset(TenMileRace, sex == "M")
f.runners = subset(TenMileRace, sex == "F")
mean(m.runners$net)
m.runners = subset(TenMileRace, sex == "M")
f.runners = subset(TenMileRace, sex == "F")
mean(m.runners$net)
mean(f.runners$net)
m.runners = subset(TenMileRace, sex == "M")
f.runners = subset(TenMileRace, sex == "F")
median(m.runners$net)
median(f.runners$net)
m.runners = subset(TenMileRace, sex == "M")
f.runners = subset(TenMileRace, sex == "F")
mean(m.runners$net)
mean(f.runners$net)
m.runners = subset(TenMileRace, sex == "M")
f.runners = subset(TenMileRace, sex == "F")
mean(m.runners$net)
mean(f.runners$net)
t.test(m.runners$net, f.runners$net)
